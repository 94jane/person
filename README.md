### 使用方法
1. 登录你的 GitHub 仓库 `94jane.github.io`
2. 点击 `Add file` → `Create new file`
3. 文件名填写 `README.md`（**注意大小写和后缀**，必须是 `.md`）
4. 把上面的代码全部粘贴到编辑区域
5. 填写提交信息（比如 `添加README.md说明文档`），点击 `Commit new file`
 提交后，仓库首页会自动显示这份README，同时不影响简历页面的访问（访问地址还是 `https://94jane.github.io/`）。
